---
default: patch
---

Avoid running passive web-push startup and resume reconciliation on browsers that do not support the Push API, while preserving the existing behavior on supported web-push clients.
