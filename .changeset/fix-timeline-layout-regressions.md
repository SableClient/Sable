---
default: patch
---

Fix recent timeline layout regressions by restoring composer vertical alignment, preserving the avatar gutter in modern message rows, and slightly increasing bundled Twemoji sizing to better match adjacent text and custom emoji.
