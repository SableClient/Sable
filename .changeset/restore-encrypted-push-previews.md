---
'charm': patch
---

Preserve decrypted event types in supported push notification paths so encrypted-room notifications can show the intended preview text instead of always falling back to generic encrypted-message copy.
